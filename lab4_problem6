#include <iostream>

using namespace std;

int main()
{
    int a = 0;
    int b = 0;
    int c = 0;
    cin>>a>>b>>c;
    
    if ((a+b>c)&&(b+c>a)&&(a+c>b)){
        cout<<"The triangle is valid";
    } else{
        cout<<"The triangle is not valid";
    }
    
    return 0;
}
